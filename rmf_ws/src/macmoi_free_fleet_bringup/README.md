# macmoi_free_fleet_bringup

This package stores the AIML Lab free_fleet runtime bringup files:

- fleet configuration YAMLs
- wrapper launch files for the free_fleet adapter

Static maps and nav graphs live in `macmoi_assets`.
